# The app targets SDK 37; Robolectric 4.16.1 ships Android images up to 36 and
# refuses to run against a target it has no image for. Nothing under test here
# is version-sensitive — it is SharedPreferences defaulting — so the tests run
# on the newest image Robolectric has. Raise this when Robolectric catches up.
sdk=36
